# 练习 A.1：返回两个数之和

## 题目说明

编写函数 `py_learn`，接收两个整数参数 `a` 和 `b`，返回它们的和。

## 示例

```python
py_learn(1, 2)  # 返回 3
py_learn(10, 20)  # 返回 30
py_learn(-5, 5)  # 返回 0
```

## 提示

- 使用 `return` 语句返回结果
- `+` 运算符可以计算两个整数的和

## 要求

- 函数签名：`def py_learn(a, b):`
- 返回值类型：`int`
