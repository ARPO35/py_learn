# 练习 A.2：打印问候语

## 题目说明

编写函数 `py_learn`，接收一个字符串参数 `name`，使用 `print` 输出问候语。

## 示例

```python
py_learn("小明")
# 输出：你好，小明
```

## 提示

- 使用 `print()` 函数输出内容
- 可以使用 f-string 格式化字符串：`f"你好，{name}"`

## 要求

- 函数签名：`def py_learn(name):`
- 使用 `print()` 输出，不需要 `return`
